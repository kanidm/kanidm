# This should be at /etc/kanidm/config or ~/.config/kanidm, and configures the kanidm command line tool
uri = "https://idm.example.com"
verify_ca = true
verify_hostnames = true

# disable the ncurses menu to prompt users for the token they want to use when multiple tokens exist
prompt_user_tokens = false
